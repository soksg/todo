<h1>Tasks</h1>
<%= link_to "新規作成", new_task_path %>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= check_box_tag '', '' %>
      <%= task.title %>
      <%= link_to "[編集]", edit_task_path(task) %>
    </li>
  <% end %>
</ul>